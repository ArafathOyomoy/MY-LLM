package org.tensorflow.lite.support.common

import android.content.Context
import java.nio.MappedByteBuffer

object FileUtil {
    fun loadMappedFile(context: Context, path: String): MappedByteBuffer? {
        return null
    }
}
